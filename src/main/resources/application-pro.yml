spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gdemov1?useUnicode=true&characterEncoding=utf-8
    username: root
    password: asd
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.avan.movie: info
  file: log/pro.log
server:
  port: 8080